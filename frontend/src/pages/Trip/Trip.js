import './Login.css';
import React, { useState } from 'react';
import { 
  createTheme, 
} from '@mui/material';

const url = 'http://127.0.0.1:5000/trip/:'
const theme = createTheme({
  palette: {
    primary: {
      main: '#CF7D30',
      darker: '#BE6C20'
    }
  }
});


const Trip = (props) => {

    return (
        <div> 
        </div>
  )};
  
  export default Trip;